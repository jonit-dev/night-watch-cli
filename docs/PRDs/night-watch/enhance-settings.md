settings sidemenu option is too poor. Must be able to properly configure the project's night-watch.config.json like custom provider env vars (like adding GLM-5), telegram config, etc

night-watch.config.json

Add proper internal tabs

Remove dead code
