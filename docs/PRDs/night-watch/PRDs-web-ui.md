on PRDs web UI page:

- Dont show done by default, only ready.
- Remove "New PRD" button
- remove sort button, leave only click on header to sort
- add "actions" column, with a play icon to execute it, stop, retry, etc. check recent commits with functionality we may use
